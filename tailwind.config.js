/** @type {import('tailwindcss').Config} */
export default {
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
    // Dark mode is now handled via CSS classes
    // Theme extensions are now in CSS using @layer utilities
};
